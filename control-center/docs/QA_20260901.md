# 2026-09-01 发布检查

## 自动检查

- Python 后端与 Uplink Manager 语法通过。
- 20 项离线单元测试通过。
- 前端共 356 个 ID，无重复。
- 内联 JavaScript 通过 Node.js 语法检查。
- 旧设备版本只读 API 兼容检查通过：7/7 必要服务、Wi-Fi 不自启、USB 管理模式正常。

## 视觉检查

新增“版本与构建”区域已检查：

- 1920 × 1080 桌面宽屏；
- 1180 × 820 iPad 横屏；
- 820 × 1180 iPad 竖屏；
- 390 × 844 手机宽度。

手机端初次检查发现长版本字符串可能撑宽页面，已增加组件构建值的宽度约束与省略显示，复查后无横向溢出。

截图位于 `docs/qa/`。

## 本轮未执行

- 蜂窝注册或流量负载；
- 短信收发；
- SIM/eSIM/APDU/AKA；
- 温度压力测试；
- 代理节点测速或订阅更新。
