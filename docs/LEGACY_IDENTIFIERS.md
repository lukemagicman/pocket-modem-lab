# Legacy identifiers

Pocket Modem Lab 是本仓库的项目名称。

源码中仍可见 `openstick`，原因包括：

- 已部署设备上的 systemd unit、目录和配置格式需要保持升级兼容；
- Android `applicationId` 已用于实机安装，直接修改会被系统视为另一个应用；
- 个别研究记录描述了作为技术基础或对照对象的上游镜像。

新增的对外文档、发布标题和仓库名称应使用 **Pocket Modem Lab**。兼容标识只在有迁移和回滚方案时修改。

